const mainRouter = require('../controllers/mainRouter.server.controller'), 
      express = require('express'),
      router = express.Router();

/* login with passport authentication */
//router.route('/')
//    .get(mainRouter.root);

module.exports = router;
